{
  "name": "stoic-meditations",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "generate:book-pdf": "node scripts/generate-book-pdf.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "monthly:summary": "node scripts/monthly-summary.js"
  },
  "devDependencies": {
    "playwright": "^1.52.0",
    "prisma": "^6.19.3"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
